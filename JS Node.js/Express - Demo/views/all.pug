extends shared/_master

block scripts
    script(src="/static/js/all.js")

block body
    h1
        | All
    ul
        each item in model
            li
                a(href="/" + item.id)
                    | Go to #{item.name}